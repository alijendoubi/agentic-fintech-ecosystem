# Runtime dependencies (ranges pinned to majors that were tested: numpy 2.5, pandas 2.3, scipy 1.x)
numpy>=2.0,<3
pandas>=2.2,<3
scipy>=1.14,<2
# America/New_York zone data for the regular-trading-hours filter (Windows ships no tz database)
tzdata>=2024.1

# Optional: Parquet input (data.read_bar_frame). Not needed for CSV. Not exercised by the tests.
# pyarrow>=15,<25
